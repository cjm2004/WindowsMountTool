# 项目文件保留与清理审查报告

**项目**：Windows 云盘挂载向导（winmount）  
**审查时间**：2026-09-13 00:32 之后  
**审查方式**：只读盘点、内容抽查、构建关系核对、重复文件哈希核对  
**本轮动作**：未删除、未移动、未重命名任何文件  

## 一、审查目标

本次审查以以下后续场景为判断依据：

1. 未来继续使用 AI 升级和优化功能，需要保留能够帮助 AI 理解项目的源码、文档、测试和重要日志。
2. 计划开源项目，需要保留可构建、可运行、可发布的源码和发布资源。
3. 清理旧版、缓存、重复副本和不再参与当前构建的文件，减少项目体积和开源泄露风险。
4. 区分“本地可以删除”和“开源仓库不应提交”。这两者不是同一个结论：有些文件应从公开仓库排除，但仍适合留在本地备查。

## 二、完整盘点概况

当前项目根目录为：

```text
C:\Users\22303\Desktop\winmount
```

只读统计结果：

| 范围 | 文件数 | 目录数 | 文件总大小 |
|---|---:|---:|---:|
| 当前项目 | 2375 | 184 | 5,207,291,989 字节，约 4.85 GiB |
| `_archive\` | 2249 | 约占绝大多数 | 3,939,048,640 字节，约 3.67 GiB |
| 当前源码、文档、测试等非归档部分 | 约 126 | 少量 | 约 1.27 GB，其中主要是工具二进制和运行测试资源 |

当前主要目录占用：

| 目录 | 文件数 | 大小 | 初步结论 |
|---|---:|---:|---|
| `_archive\` | 2249 | 约 3.67 GiB | 历史资料和缓存，优先清理候选，但不建议未经确认直接删除 |
| `py_src_wizard\` | 14 | 约 567 MiB | 当前配置向导源码、构建输出和内嵌工具，核心保留 |
| `runtime\` | 3 | 约 192 MiB | 本机运行测试资源，需保留或移出公开仓库 |
| `py_src\` | 13 | 约 230 MiB | 当前安装器源码、payload 和构建输出，核心保留 |
| 根目录两个 EXE | 2 | 约 205 MiB | 正式发布成品，必须保留 |
| `tools\` | 21 | 约 13.4 MiB | 构建工具和 WinFsp 安装包，建议保留 |
| `logs\` | 51 | 约 308 KiB | AI 维护和问题追踪资料，建议保留但开源前脱敏 |
| `docs\` | 8 | 约 73 KiB | 项目文档和历史决策，必须/建议保留 |
| `tests\` | 7 | 约 43 KiB | 自动化测试脚本，必须保留 |
| `.workbuddy\` | 5 | 约 35 KiB | 本地项目工作记忆，不作为开源内容提交 |

## 三、项目当前构建链

### 1. 当前正式构建链

当前可继续使用和维护的构建链是 Python/Tkinter + PyInstaller：

```text
py_src_wizard/app.py
        │
        ▼ PyInstaller + py_src_wizard/mount_wizard.spec
py_src_wizard/dist/mount_wizard.exe
        │
        ▼ 经过验证后复制
py_src_wizard/payload/mount_wizard.exe
py_src/payload/mount_wizard.exe
        │
        ▼ PyInstaller + py_src/CloudMountSetup.spec
py_src/dist/CloudMountSetup.exe
        │
        ▼ 验证后同步
根目录 CloudMountSetup.exe
```

当前正式副本核对结果：

- `mount_wizard.exe`、`py_src_wizard/payload/mount_wizard.exe`、`py_src/payload/mount_wizard.exe` 三份哈希一致：
  `9c19b3f03dcc51514e5de1a02658879dc2adc07598f28b8c3f87f77073b7ac4e`
- `CloudMountSetup.exe` 与 `py_src/dist/CloudMountSetup.exe` 两份哈希一致：
  `8ab85e08d21cb341d930d2ba71a400a7f65d1de3cea9ad24cb454ac65490e686`
- `py_src_wizard/dist/mount_wizard.exe` 哈希为：
  `f46a248e50dbf792048cd61af8774629bca2247d4d355e1d79abaa6bedd3fa25`
- 因此 `py_src_wizard/dist/mount_wizard.exe` 是较早构建输出，不应替换当前正式 payload 或根目录成品。

### 2. 已清理的旧版构建链

此前已按确认将以下内容移入 Windows 回收站：

- `source\`：旧版 PowerShell/NSIS 源码链。
- `build.ps1`：旧版 NSIS 构建入口。
- 根目录 `dist\`：旧版发布副本和重复资源。

因此当前 `README.md`、`docs\目录说明.md`、`docs\QA_REPORT.md`、`docs\FIXES_PRIORITY.md`、`docs\replace_icon.txt` 以及部分日志中仍出现 `source\` 或根目录 `dist\` 的引用。这些引用主要是历史记录，但开源前应更新说明，避免新贡献者按照已不存在的路径构建。

## 四、必须保留

“必须保留”是指删除后会直接破坏当前构建、运行、开源复现或 AI 后续维护能力的内容。

### A. 当前 Python 源码

| 路径 | 用途 | 删除风险 |
|---|---|---|
| `py_src\installer.py` | 当前 Tkinter 安装器界面、安装流程、资源释放、快捷方式、卸载项和升级处理 | 无法继续维护或重建 `CloudMountSetup.exe` |
| `py_src\ui_theme.py` | 安装器共用主题模块 | 安装器主题和控件样式无法正常维护 |
| `py_src\CloudMountSetup.spec` | PyInstaller 安装器打包配置和 payload 声明 | 无法稳定重建安装器，内嵌资源关系丢失 |
| `py_src_wizard\app.py` | 当前配置向导、控制台、Alist/rclone/WinFsp 挂载逻辑 | 无法升级核心功能，AI 也无法理解或修改主程序 |
| `py_src_wizard\ui_theme.py` | 配置向导共用主题模块 | 配置向导界面维护和主题同步会中断 |
| `py_src_wizard\mount_wizard.spec` | PyInstaller 挂载向导打包配置 | 无法重建配置向导 |
| 根目录 `ui_theme.py` | 当前项目保留的主题模块副本和静态检查参考 | 删除前必须确认没有脚本、测试或开源说明依赖；当前建议保留 |

### B. 当前构建所需 payload 和发布资源

| 路径 | 用途 | 删除风险 |
|---|---|---|
| `py_src\payload\mount_wizard.exe` | 安装器内嵌的配置向导正式副本 | 重建安装器时会缺少主程序，或把旧版本打进安装包 |
| `py_src\payload\winfsp.msi` | 安装器内嵌的 WinFsp 安装资源 | 用户无法离线安装 WinFsp，安装包功能不完整 |
| `py_src\payload\THIRD_PARTY_NOTICES.txt` | 安装包内第三方组件声明 | 开源组件再分发时缺少许可和归属说明 |
| `py_src_wizard\payload\source\tools\alist.exe` | 配置向导内嵌 Alist | 安装后无法提供本地 Alist/WebDAV 服务 |
| `py_src_wizard\payload\source\tools\rclone.exe` | 配置向导内嵌 rclone | 无法执行 WebDAV 到本地盘符的挂载 |
| `py_src_wizard\payload\source\config\rclone.conf` | 无密码的默认配置模板 | 删除会导致构建时缺少默认配置模板 |
| `py_src_wizard\payload\source\assets\wechat_donation.png` | 关于页微信二维码 | 关于页缺少该资源，构建结果不完整 |
| `py_src_wizard\payload\source\assets\alipay_donation.png` | 关于页支付宝二维码 | 关于页缺少该资源，构建结果不完整 |
| `py_src\icon.ico` | 安装器图标 | 重建安装器时图标缺失或回退为默认图标 |
| `py_src_wizard\icon.ico` | 配置向导图标 | 重建挂载向导时图标缺失 |

### C. 当前正式成品

| 路径 | 用途 | 删除风险 |
|---|---|---|
| `CloudMountSetup.exe` | 对外发布和最终安装入口 | 没有现成安装包，开源发布无法直接提供可用成品 |
| `mount_wizard.exe` | 对外发布的配置向导成品 | 用户无法直接使用配置和挂载功能 |
| `py_src\dist\CloudMountSetup.exe` | 当前安装器构建输出和正式副本 | 只保留根目录成品也能使用，但失去构建结果对照和快速恢复副本 |
| `py_src_wizard\dist\mount_wizard.exe` | 最近一次向导构建输出 | 当前文件哈希与正式 payload 不一致，必须先确认版本；建议保留用于版本追溯，不作为发布副本 |

### D. 测试脚本

以下文件应保留，用于 AI 后续升级后的回归测试和开源贡献者验证：

- `tests\01_install.ps1`：安装流程测试。
- `tests\02_mount.ps1`：挂载链路测试。
- `tests\03_uninstall.ps1`：卸载流程测试。
- `tests\diag_mount.ps1`：挂载问题诊断。
- `tests\probe_alist_api.ps1`：Alist 管理接口探测。
- `tests\comprehensive_test.ps1`：综合测试。
- `tests\full_functional_test.ps1`：完整功能测试。

删除风险：后续修改安装、挂载、卸载、WinFsp 检测或 Alist 接口后，缺少现成回归入口，AI 也难以验证改动是否破坏核心链路。

## 五、建议保留

“建议保留”是指不一定参与程序启动，但对 AI 后续升级、问题追踪、开源维护、合规发布或本地恢复有明显价值。

### A. 项目文档

建议保留整个 `docs\`，具体作用如下：

| 路径 | 用途 | 删除风险 |
|---|---|---|
| `docs\目录说明.md` | 当前目录结构、构建链、保留边界和归档规则 | 新协作者容易误用旧目录或删除重要 payload |
| `docs\Windows挂载网盘器_优化后提示词.md` | 需求、硬约束、排错经验和 AI 开发上下文 | AI 后续升级会丢失关键历史约束，例如 WinFsp、rclone 配置和 Alist API 坑位 |
| `docs\DEV_LOG.md` | 开发过程和技术决策时间线 | 难以追溯为什么采用当前架构和改动方式 |
| `docs\FIXES_PRIORITY.md` | 历史问题和修复优先级 | 可能重复排查已经发现的问题，或遗漏未完成事项 |
| `docs\HOW_TO_ADD_ICON.md` | 图标资源处理方法 | 后续发布图标调整需要重新摸索 |
| `docs\ICON_SOLUTION.md` | 图标问题与解决记录 | 失去发布资源处理的历史依据 |
| `docs\QA_REPORT.md` | 质量检查、风险和历史测试结论 | AI 后续升级缺少已知缺陷和测试基线 |
| `docs\replace_icon.txt` | Resource Hacker 图标替换记录 | 仅在旧的图标处理流程仍需复盘时有用 |

注意：其中部分内容引用已经删除的旧 `source\` 和 `dist\`，建议保留文件本身，但开源前应更新路径或增加“历史路径”说明。

### B. `logs\` 日志

建议保留 `logs\`，但应区分长期有价值的报告和一次性过程日志。

**优先保留：**

- `logs\FINAL_TEST_REPORT.md`：完整功能测试基线。
- `logs\CODE_REVIEW_2026-09-12.md`：最近代码审查和风险记录。
- `logs\build.log`、`logs\build-launcher.log`：正式构建过程参考。
- `logs\pyinstaller_wizard_v2.log`、`logs\pyinstaller_setup_v2.log`：Python 构建过程参考。
- `logs\test-install.txt`、`logs\test-mount.txt`、`logs\test-uninstall.txt`：安装、挂载、卸载测试摘要。
- `logs\test-alist-api.txt`、`logs\test-diag.txt`：接口和诊断结果。

**可继续保留但不应公开发布：**

- `logs\install-run.log`
- `logs\mount-run.log`
- `logs\uninstall-run.log`
- `logs\final-run.log`
- `logs\final-uninstall.log`
- `logs\alist-probe-run.log`
- `logs\diag-run.log`

这些日志含有用户名 `MATEBOOKD14\22303`、本机路径、运行时间、接口返回和测试环境细节。删除风险主要是失去诊断证据；公开风险是暴露本机环境和接口信息。建议本地保留，开源前脱敏后再决定是否提交。

**可以整理但不建议现在删除：**

- `logs\_probe.txt`
- `logs\_normalize.txt`
- `logs\_winfsp*.txt`
- `logs\resource_hacker.log`
- `logs\resource_hacker_script.log`
- 旧的 `rebuild_*.log`、`build_with_*.log` 和重复构建日志

这些通常是过程记录，删除不会影响程序，但可能影响问题复盘。建议等开源仓库首次发布完成并确认没有审查需求后，再清理重复日志。

### C. `tools\`

建议保留：

- `tools\winfsp-2.1.25156.msi`：本地离线构建和安装测试所需的 WinFsp 安装包。
- `tools\ResourceHacker\`：图标和 PE 资源检查、修改工具及其许可证说明。
- `tools\resource_hacker.zip`：Resource Hacker 原始压缩包，可作为工具恢复来源。

删除风险：如果后续需要重建图标、检查 PE 资源或离线测试 WinFsp，需要重新下载工具；其中 Resource Hacker 的再分发还受其自带许可条款约束，开源仓库不一定适合直接提交。

### D. `.workbuddy\`

建议本地保留整个 `.workbuddy\`，因为其中包含项目长期记忆和每日工作日志，对 AI 后续继续维护有价值。

但它不建议进入公开仓库，原因是：

- 可能包含本机路径、用户名和历史操作信息。
- 记录的是本地协作上下文，不是产品源码。
- 其中部分内容会提到配置、令牌、密码处理和测试环境。

删除风险：AI 后续会失去项目历史上下文。开源处理方式：本地保留，仓库发布时通过 `.gitignore` 排除。

### E. `_archive\` 中的关键历史资料

建议至少保留一份外部备份或压缩归档，不必全部放在当前工作目录中：

- `_archive\extracted\指令包\云盘挂载本地盘符_引导词与排错大全.md`
- `_archive\extracted\工具包\README.txt`
- `_archive\extracted\工具包\docs\...md`
- `_archive\analysis\` 中的反汇编和重构分析资料
- `_archive\CloudMountTest\` 中可作为旧版本对照的测试成品

删除风险：无法再复盘旧版行为、原始需求和 Python 字节码逆向过程。它们不必进入公开仓库，但建议在本地备份中保留。

## 六、可以删除或清理

“可以删除”分为两种：一是对当前本地项目没有实际价值，二是已经有可靠备份且只是历史重复。即使属于此类，执行前也应再次确认备份和回收站状态。

### A. 强烈建议优先清理：历史构建缓存

路径：

```text
_archive\build_cache\
```

规模：217 个文件，约 2,762,601,545 字节，约 2.57 GiB。

内容包括：

- PyInstaller 的 `build\` 中间目录。
- 历次 `build_final\`、`build_about\`、`build_glass\`、`build_review\` 等构建中间结果。
- 历次 `dist_review\`、`dist_final\` 等旧输出。
- Python `__pycache__` 和 `.pyc`。
- PyInstaller 的 `.toc`、`.pkg`、`.pyz`、警告和交叉引用文件。

用途：只用于复盘旧构建过程，不是当前构建输入。  
删除风险：无法快速对比历史构建细节，但不会影响当前源码、payload、正式 EXE 或重新构建能力。  
建议：确认当前正式 EXE 已备份后，整个目录作为第一优先级清理对象。

### B. 可以清理：程序解包目录

路径：

```text
_archive\CloudMountSetup.exe_extracted\
_archive\mount_wizard.exe_extracted\
```

规模：共 2007 个文件，约 344,933,895 字节，约 329 MiB。

用途：分析旧版 PyInstaller 程序的内部资源、字节码和运行库。  
删除风险：失去旧版程序分析资料；当前完整 Python 源码已存在，因此恢复和升级不再依赖这些解包文件。  
建议：若不再进行逆向复盘，可删除；若希望保留历史证据，可压缩后移出项目目录。

### C. 可以清理：`_archive\analysis\`

规模：6 个文件，约 656 KiB。

用途：保存 `dis` 反汇编、字节码分析和早期重构辅助脚本。  
删除风险：AI 后续无法查看旧版反编译依据，但当前源码已重建完成。  
建议：优先压缩备份，之后可以从项目工作目录删除；如果希望保留完整技术史，则继续保留在项目外。

### D. 可以清理：旧版测试运行目录

路径：

```text
_archive\CloudMountTest\
```

规模：3 个文件，约 183,928,109 字节，约 175 MiB。

用途：旧版安装、挂载、卸载测试产生的安装器、挂载向导和第三方声明文件。  
删除风险：失去旧版二进制回归对照；不会影响当前 Python 源码和正式成品。  
建议：如果当前正式 EXE 已留存且不再对比旧版，可删除或移出项目。

### E. 可以清理：旧版安装器成品

路径：

```text
_archive\old\CloudMountSetup.exe
_archive\CloudMountSetup.7zSFX_废弃方案.exe
_archive\CloudMountSetup.原始PyInstaller版.exe
_archive\CloudMountSetup_NSIS_56MB.exe
_archive\CloudMountSetup_NSIS_root_56MB.exe
```

规模：约 374 MiB。

用途：旧版 PyInstaller、NSIS、7z SFX 和中间方案的回退或比较版本。  
删除风险：无法进行旧版安装器行为对比，也无法回退到旧版本；当前正式安装器和清理前备份仍然存在。  
建议：保留当前正式版本和一份外部备份后，其余旧版 EXE 可清理。特别是 `CloudMountSetup.7zSFX_废弃方案.exe` 已知不是正确安装器，不建议继续留在开源项目目录。

### F. 可以清理：原始 ZIP 和重复解压包

路径：

```text
_archive\云盘挂载本地盘工具包.zip
_archive\解压本压缩包，将指令复制给AIagent.zip
```

规模：约 70.7 MiB。

用途：用户最初提供的原始输入包。  
删除风险：无法重新获取原始材料；但当前源码、文档、工具和配置模板均已存在。  
建议：保留在项目外备份，不要提交公开仓库；确认备份可读后可以从工作目录清理。

### G. 可以清理：归档中的旧版发布脚本

路径：

```text
_archive\dist-legacy\Windows挂载网盘器-安装.ps1
```

用途：旧版发布脚本。  
删除风险：只能影响旧链复盘，不影响当前 Python 构建。  
建议：可删除；如果保留，则必须明确标记为 legacy，避免贡献者误用。

## 七、当前不建议删除的重复副本

有些文件看似重复，但目前仍承担构建链或发布职责，不应仅因哈希重复就删除。

### 1. `mount_wizard.exe` 的三份正式副本

以下三份当前哈希一致：

```text
mount_wizard.exe
py_src_wizard/payload/mount_wizard.exe
py_src/payload/mount_wizard.exe
```

它们分别承担：根目录发布、向导资源副本、安装器 payload。删除任意一份都可能造成发布目录或重建链不完整。

### 2. `CloudMountSetup.exe` 的两份正式副本

```text
CloudMountSetup.exe
py_src/dist/CloudMountSetup.exe
```

分别承担根目录发布和安装器构建输出对照。开源仓库可以只发布一个成品，但在当前本地项目中建议暂时保留两份。

### 3. Alist、rclone 和配置模板的三份副本

以下位置内容哈希一致：

```text
py_src_wizard/payload/source/tools/
py_src_wizard/dist/runtime/tools/
runtime/tools/
```

它们分别承担内嵌源资源、构建输出运行时和本机测试运行时。不能因为内容相同就直接删除 payload 或 runtime 中的副本。

## 八、开源仓库发布建议

“保留在本地项目”不等于“全部提交到 Git 仓库”。建议公开仓库采用以下结构：

### 建议提交

- `py_src\installer.py`
- `py_src\ui_theme.py`
- `py_src\CloudMountSetup.spec`
- `py_src\uninstaller.py`
- `py_src\uninstall.spec`
- `py_src_wizard\app.py`
- `py_src_wizard\ui_theme.py`
- `py_src_wizard\mount_wizard.spec`
- `py_src\payload\THIRD_PARTY_NOTICES.txt`
- `py_src_wizard\payload\source\config\rclone.conf`，但必须保持无真实密码、令牌和个人地址
- `py_src_wizard\payload\source\assets\` 中允许公开的项目图片
- `tests\`
- 清理并修正路径后的 `README.md`
- `docs\` 中不含本机敏感信息的设计、开发和使用文档
- `tools\` 的下载说明或校验值，是否直接提交二进制需结合许可证决定
- 根目录正式 EXE，是否提交取决于仓库体积和发布策略；建议同时提供 Release 附件

### 建议排除

- `.workbuddy\`
- `runtime\`
- `_archive\`
- `py_src\dist\`
- `py_src_wizard\dist\`
- 所有 `build*`、`dist_*`、`__pycache__`、`.pyc`
- 运行日志和包含本机路径的原始测试日志
- 原始 ZIP 包
- 解包目录
- 旧版 EXE
- Resource Hacker 二进制及其原始压缩包，除非确认许可证和仓库策略允许
- 真实的 `rclone.conf`、`profiles.json`、Alist 数据目录和任何个人配置

### 必须新增或检查

当前项目没有检测到 Git 仓库目录 `.git`，也没有现成 `.gitignore`。开源前应新增 `.gitignore`，至少排除：

```gitignore
.workbuddy/
runtime/
_archive/
**/__pycache__/
*.pyc
py_src/build/
py_src_wizard/build/
py_src/dist_review/
py_src_wizard/dist_review/
*.log
```

如果要把正式 EXE 提交到仓库，需要明确是提交到 Git LFS、Release 附件还是普通 Git；不要把所有历史 EXE 和构建缓存一起提交。

## 九、明确的推荐清理清单

以下是基于当前目标的推荐顺序。本报告只给清单，不执行清理。

### 第一批：低风险、高收益

建议在确认备份后清理：

```text
_archive\build_cache\
_archive\dist-legacy\
_archive\CloudMountSetup.7zSFX_废弃方案.exe
```

原因：全部是历史构建缓存、旧发布脚本或已确认废弃的错误安装包；不参与当前构建。

### 第二批：可删除但需确认历史价值

```text
_archive\CloudMountSetup.exe_extracted\
_archive\mount_wizard.exe_extracted\
_archive\analysis\
_archive\CloudMountTest\
```

原因：它们主要服务于早期逆向分析和旧版回归。当前源码已经存在，AI 后续升级不再依赖解包目录；如果仍想保留技术史，先压缩并移出项目目录。

### 第三批：确认外部备份可恢复后清理

```text
_archive\old\
_archive\CloudMountSetup.原始PyInstaller版.exe
_archive\CloudMountSetup_NSIS_56MB.exe
_archive\CloudMountSetup_NSIS_root_56MB.exe
_archive\云盘挂载本地盘工具包.zip
_archive\解压本压缩包，将指令复制给AIagent.zip
```

原因：均为旧成品或原始输入材料。删除后主要损失回退和复盘能力，不能在没有外部备份的情况下直接处理。

### 本次明确不建议清理

```text
CloudMountSetup.exe
mount_wizard.exe
py_src\
py_src_wizard\
py_src\payload\
py_src_wizard\payload\
py_src\CloudMountSetup.spec
py_src_wizard\mount_wizard.spec
tests\
docs\
logs\
tools\
runtime\
.workbuddy\
```

其中 `runtime\` 和 `.workbuddy\` 不建议进入公开仓库，但它们对本地继续使用和 AI 维护有价值，因此“本地保留、开源排除”。

## 十、开源前必须修正的问题

### 1. 修正文档中的旧路径

需要处理以下仍引用已删除旧链的文件：

- `README.md`
- `docs\目录说明.md`
- `docs\QA_REPORT.md`
- `docs\FIXES_PRIORITY.md`
- `docs\replace_icon.txt`
- `logs\FINAL_TEST_REPORT.md`
- `logs\CODE_REVIEW_2026-09-12.md`
- `logs\ps-syntax.txt`
- 与旧构建相关的其他历史日志

历史报告可以保留原文，但应在报告开头标注“历史记录，路径可能已归档或删除”。面向新贡献者的 README 和目录说明必须改成当前 `py_src` / `py_src_wizard` 构建流程。

### 2. 检查并脱敏日志

已发现日志中出现：

- Windows 用户名和计算机名。
- `C:\Users\22303\...` 等本机路径。
- `C:\CloudMountTest`、`C:\CloudMountTestData` 等测试路径。
- Alist API 返回、WebDAV 地址和测试时序。

当前配置模板 `rclone.conf` 的 `pass =` 为空，未发现实际密码，但发布前仍应检查所有日志和历史文件，确认没有 token、密码、Cookie 或个人 WebDAV 地址。

### 3. 不要把 `runtime\config\rclone.conf` 当作公开配置

当前三份 `rclone.conf` 是无密码模板，暂未发现真实凭据；但 `runtime\` 的职责是本机运行数据，建议通过 `.gitignore` 排除，而不是依赖人工判断每次是否安全。

### 4. 保留第三方许可说明

`py_src\payload\THIRD_PARTY_NOTICES.txt` 应随发布成品保留。Alist、rclone、WinFsp、Python、Tkinter、PyInstaller、pywinstyles 等组件的许可证和来源需要在开源仓库或 Release 中明确说明。Resource Hacker 是否分发需单独核对其自带许可。

## 十一、最终结论

### 必须保留

当前 Python 源码、两个 PyInstaller spec、构建 payload、第三方声明、测试脚本、根目录正式 EXE，以及用于继续维护的核心文档。

### 建议保留

`docs\`、重要测试报告、关键构建日志、`tools\`、`.workbuddy\`、`runtime\` 和 `_archive\` 中的原始需求/排错资料。它们适合本地维护，但不应全部提交开源仓库。

### 可以删除

`_archive\build_cache\`、旧解包目录、旧分析目录、旧版测试成品、旧版安装器、旧版发布脚本和原始 ZIP。推荐先从构建缓存开始，先清理约 2.57 GiB；其他历史资料在确认外部备份后再处理。

本次审查没有执行任何清理动作。后续如果执行清单，应继续使用备份、逐批处理、回收站和逐批核验流程。
